/* the open and close of the nav */
function openNav() {
    document.getElementsByClassName("nav")[0].style.transform = "translate(0)";
}

function closeNav() {
    document.getElementsByClassName("nav")[0].style.transform = "translate(-100%)";
}
