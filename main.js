/* Open when someone clicks on the span element */
function openNav() {
    document.getElementsByClassName("nav_area")[0].style.width = "100%";
}

/* Close when someone clicks on the "x" symbol inside the overlay */
function closeNav() {
    document.getElementsByClassName("nav_area")[0].style.width = "0%";
}